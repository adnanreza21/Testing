<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.small.testing">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.small.testing.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.small.testing.ExitActivity" />
        <activity android:name="com.small.testing.SchoolActivity" />
        <activity android:name="com.small.testing.BathActivity" />
        <activity android:name="com.small.testing.EatActivity" />
        <activity android:name="com.small.testing.StudyActivity" />
        <activity android:name="com.small.testing.MainActivity">

        </activity>
    </application>

</manifest>